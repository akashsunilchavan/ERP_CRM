import { Harvesting } from '@/src/components/harvesting/Harvesting';
import React from 'react';

const page = () => {
    return (
        <div>
            <Harvesting />
        </div>
    );
};

export default page;
